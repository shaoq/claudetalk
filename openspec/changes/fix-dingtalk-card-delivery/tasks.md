## 1. 卡片投放上下文建模

- [x] 1.1 在 `src/types.ts` 中新增或整理 AI 卡片投放上下文类型，明确包含 `conversationType`、`conversationId`、`senderStaffId`
- [x] 1.2 修正 AI 卡片创建请求类型定义，使字段名和结构与实际钉钉接口请求保持一致
- [x] 1.3 清理仍将卡片创建请求建模为单一 `conversationId` 的旧类型或旧注释，避免后续误用

## 2. 钉钉卡片创建请求重构

- [x] 2.1 在 `src/dingtalk.ts` 中重构 `createAICard` 入参，改为接收结构化投放上下文
- [x] 2.2 按私聊与群聊场景分别构造正确的 `openSpaceId`
- [x] 2.3 在卡片创建请求中补齐与场景对应的 open space model 和 deliver model
- [x] 2.4 为私聊缺少 `senderStaffId` 的情况增加显式校验和可诊断错误
- [x] 2.5 将卡片创建成功判定改为基于 `response.ok` 和业务错误字段，而不是固定状态码

## 3. 调用链接入与错误可观测性

- [x] 3.1 在 `src/index.ts` 的卡片模式调用点传入完整的卡片投放上下文，而不是仅传 `chatId`
- [x] 3.2 保持 markdown 回复路径不变，确保本次改动只影响卡片模式
- [x] 3.3 完善卡片创建失败日志，记录 HTTP 状态、远端错误码和错误消息

## 4. 验证与回归

- [ ] 4.1 验证私聊场景下 AI 卡片可以成功创建，不再出现 `param.openSpaceIdInvalid`
- [ ] 4.2 验证群聊场景下 AI 卡片创建请求仍能正常投放
- [ ] 4.3 验证钉钉接口返回非 2xx 或业务错误时，系统会抛出包含远端信息的错误
- [ ] 4.4 验证卡片流式更新链路在卡片成功创建后仍可继续工作
