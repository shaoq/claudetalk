## 1. 协议对照

- [x] 1.1 对照 `dingtalk-openclaw-connector` 与钉钉官方文档，梳理当前 `createAICard` / `streamAICard` 在私聊场景下的字段差异
- [x] 1.2 确认参考实现在私聊场景使用的目标标识语义，并据此确定 ClaudeTalk 应使用的回调字段组合

## 2. 运行时模型调整

- [x] 2.1 扩展 `src/types.ts` 中 AI 卡片创建/更新请求与响应类型，补齐实例标识、目标上下文和更新选项字段
- [x] 2.2 调整 `src/dingtalk.ts` 中的 `AICardInstance` 运行时模型，使其保留后续更新与诊断所需的完整上下文

## 3. 卡片协议实现

- [x] 3.1 重构 `src/dingtalk.ts#createAICard`，按对齐后的私聊目标和创建协议构造请求并提取完整卡片上下文
- [x] 3.2 重构 `src/dingtalk.ts#streamAICard`，显式发送按 key 更新卡片数据的请求语义，并补齐最终结束所需字段
- [x] 3.3 更新 `src/index.ts` 中实验路径和正式卡片路径，确保后续更新使用新的卡片上下文

## 4. 验证与回归

- [ ] 4.1 用真实钉钉私聊运行固定文本两阶段实验，验证阶段一文本首屏可见
- [ ] 4.2 在同一次实验中验证阶段二更新能覆盖同一张卡片的 `content` 字段
- [ ] 4.3 固定文本实验通过后，回归正式 Claude 流式卡片路径并记录是否仍存在剩余体验问题
